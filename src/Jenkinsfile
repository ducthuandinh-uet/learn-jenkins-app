pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
        stage('npn test'){
            steps{
                sh 'npm --version'
            }
        }
    }
}
